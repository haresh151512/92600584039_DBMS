--EX-3.WRITE A PL/SQL BLOCK TO CALCULATE THE TOTAL, PERCENTAGE AND GRADE OF STUDENT BASED 
       ON HIS/HER ROLLNO FROM RESULT TABLE. (CREATE RESULT TABLE WITH ROLLNO, NAME, SUB1, SUB2, 
       SUB3, SUB4, SUB5, TOTAL, PER, GRADE ATTRIBUTES WITH APPROPRIATE DATA TYPE).
*/

--------------------
--CREATE TABLE--

CREATE TABLE RESULT
(
    ROLLNO NUMBER(5) PRIMARY KEY,
    NAME   VARCHAR2(50),
    SUB1   NUMBER(3) CHECK (SUB1 BETWEEN 0 AND 100),
    SUB2   NUMBER(3) CHECK (SUB2 BETWEEN 0 AND 100),
    SUB3   NUMBER(3) CHECK (SUB3 BETWEEN 0 AND 100),
    SUB4   NUMBER(3) CHECK (SUB4 BETWEEN 0 AND 100),
    SUB5   NUMBER(3) CHECK (SUB5 BETWEEN 0 AND 100),
    TOTAL  NUMBER(5),
    PER    NUMBER(5,2),
    GRADE  VARCHAR2(10)
);
--------------------

--------------------
--INSERT DATA--

INSERT INTO RESULT (ROLLNO, NAME, SUB1, SUB2, SUB3, SUB4, SUB5)
VALUES (1, 'VIVEK', 91, 95, 85, 84, 86);

INSERT INTO RESULT (ROLLNO, NAME, SUB1, SUB2, SUB3, SUB4, SUB5)
VALUES (2, 'RAHUL', 78, 69, 80, 75, 72);

INSERT INTO RESULT (ROLLNO, NAME, SUB1, SUB2, SUB3, SUB4, SUB5)
VALUES (3, 'PRIYA', 90, 92, 95, 88, 91);

--------------------

SET SERVEROUTPUT ON;

DECLARE
    RNO RESULT.ROLLNO%TYPE;
    SNAME RESULT.NAME%TYPE;
    S1 RESULT.SUB1%TYPE;
    S2 RESULT.SUB2%TYPE;
    S3 RESULT.SUB3%TYPE;
    S4 RESULT.SUB4%TYPE;
    S5 RESULT.SUB5%TYPE;
    TOTAL RESULT.TOTAL%TYPE;
    PER RESULT.PER%TYPE;
    GRADE RESULT.GRADE%TYPE;

BEGIN
    RNO := &ENTER_ROLL_NO;

    SELECT NAME, SUB1, SUB2, SUB3, SUB4, SUB5
    INTO SNAME, S1, S2, S3, S4, S5
    FROM RESULT
    WHERE ROLLNO = RNO;

    TOTAL := S1 + S2 + S3 + S4 + S5;
    PER := TOTAL / 5;

    -- CHECK PASS/FAIL AND GRADE
    IF (S1 < 35 OR S2 < 35 OR S3 < 35 OR S4 < 35 OR S5 < 35) THEN
        GRADE := 'F';
        DBMS_OUTPUT.PUT_LINE('RESULT : FAIL');
    ELSE
        IF PER >= 90 THEN
            GRADE := 'A';
        ELSIF PER >= 70 THEN
            GRADE := 'B';
        ELSIF PER >= 60 THEN
            GRADE := 'C';
        ELSIF PER >= 50 THEN
            GRADE := 'D';
        ELSE
            GRADE := 'E';
        END IF;

        DBMS_OUTPUT.PUT_LINE('RESULT : PASS');
    END IF;

    UPDATE RESULT
    SET TOTAL = TOTAL, PER = PER, GRADE = GRADE
    WHERE ROLLNO = RNO;

    DBMS_OUTPUT.PUT_LINE('------------------------------------');
    DBMS_OUTPUT.PUT_LINE('ROLL NO      : ' || RNO);
    DBMS_OUTPUT.PUT_LINE('NAME         : ' || SNAME);
    DBMS_OUTPUT.PUT_LINE('TOTAL        : ' || TOTAL);
    DBMS_OUTPUT.PUT_LINE('PERCENTAGE   : ' || PER || '%');
    DBMS_OUTPUT.PUT_LINE('GRADE        : ' || GRADE);
    DBMS_OUTPUT.PUT_LINE('------------------------------------');
END;
/